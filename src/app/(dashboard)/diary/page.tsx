import { SectionPage } from '@/components/section-page'

export default function DiaryPage() {
  return (
    <SectionPage
      title="Nhật ký"
      description="Route riêng cho timeline nhật ký, chuẩn bị migrate từ UI cũ sang App Router."
    />
  )
}
